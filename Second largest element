class Solution {
    int print2largest(int arr[], int n) {
        int result=-1;
        Arrays.sort(arr);
        for(int i=0; i<n; i++)
        {
            if(arr[i]!=arr[n-1])
            result=arr[i];
    }
    return result;}
}
